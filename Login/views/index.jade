extends layout 

block content 
    h1= title
    p Hello #{member} 
        if logstatus == false 
            a(href='login.html') 登入
        else 
            a(href='./logout') 登出
        , Welcome to #{title}
    p 未斷線（登出）情況，已 #{time} 次登入！